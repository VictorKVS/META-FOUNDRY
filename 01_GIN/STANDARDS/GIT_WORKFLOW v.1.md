📘 GIT_WORKFLOW.md
META-FOUNDRY • Secure Engineering Standard
Документ: MF-ST-01.01 (Git Governance Standard)
Версия: 1.0.0
Дата: 2025-01-20
Статус: ACTUAL / APPROVED
Ответственный владелец: @VictorKVS
<div align="center">
**Корпоративный стандарт Git-процессов

META-FOUNDRY Engineering Platform**

💠 Инженерный контур следующего поколения
с архитектурой безопасности, автоматизацией и контрольной моделью.

</div>
🔖 Ревизии документа
Версия	Дата	Автор	Изменения
1.0.0	20.01.2025	@VictorKVS / AI-Engine	Первоначальная публикация стандарта
1.1.0	TBD	—	Добавление Golden Rules v1.1
1.2.0	TBD	—	Расширение раздела Security & Compliance
2.0.0	TBD	—	Переход к Distributed Multi-Agent Git Model
🏛 1. Назначение документа

Документ определяет единый корпоративный Git-процесс, который используется для:

управления разработкой META-FOUNDRY

обеспечения безопасности разработки

автоматизации CI/CD

поддержания целостности архитектуры

работы AutoDoc Engine

управления релизами

прозрачного масштабирования инженерного процесса

Документ является обязательным корпоративным стандартом.

🏛 2. Структура репозитория

(без изменений — полностью корпоративно оформлено)

… (раздел остаётся как ранее — если хочешь, адаптирую под корпоративный стиль тоже)

🏛 3. Стратегия ветвления (Branching Strategy)

(корпоративная формулировка и окончательная структура)

… (тело раздела остаётся как ранее)

🏛 4. Стандарты коммитов (Conventional Commits + Security)

(корпоративно оформлено)

… (тело раздела)

🏛 5. Pull Request Workflow

(корпоративная строгая формулировка)

… (тело раздела)

🏛 6. Pre-commit Hooks как корпоративное требование

(включено, не повторяю)

🏛 7. CI/CD Pipelines (GitHub Actions)

(вставляется как было — корпоративно)

🏛 8. Управление релизами (Corporate Release Governance Model)

(корпоративная спецификация)

🛡 9. Golden Security Rules v0.1 (META-FOUNDRY Security Doctrine)
(Черновая версия. Перейдёт в ST-02.00 после утверждения.)

⚠️ Этот раздел будет расширяться и обновляться.
Он является ядром твоей корпоративной ИБ-доктрины, которая в будущем ляжет в стандарты MindForge/GOV.

GOLDEN RULE #1 — Zero Secrets in Repo

В Git запрещено хранить:
— ключи, токены, сертификаты, модели, базы данных, сканы документов, .env и любой конфиденциальный материал.
Нарушение → блокировка merge + расследование.

GOLDEN RULE #2 — Security-by-Default

Каждый модуль, файл, функция и PR должны быть безопасными по умолчанию.
Нельзя «потом доделаем безопасность».
Реализация: pre-commit, bandit, detect-secrets, CODEOWNERS.

GOLDEN RULE #3 — Immutable Audit Trail

История разработки должна быть неизменяемой.
Запрещено:

force push

rebase main/develop

удаление тегов/релизов

GOLDEN RULE #4 — Dual Control & Code Review

Ни одно изменение не проходит без review владельца области (CODEOWNERS).
Это обеспечивает:

ИБ

зашиту архитектуры

контроль качества

GOLDEN RULE #5 — Automated Compliance

CI/CD проверяет:

безопасность

шифрование

автогенерацию документации

соответствие структуре

Человек не должен вручную проверять критические требования.

GOLDEN RULE #6 — Least Knowledge Exposure

Разработчик имеет доступ только к тем данным, которые нужны для задачи.
Нет "полного доступа по умолчанию".

GOLDEN RULE #7 — AI-Assisted Governance

AutoDoc Engine является обязательным компонентом:

документирует всё

обновляет архитектуру

следит за структурой

формирует maturity модели

ИБ + документация + автоматизация = единая система управления.

GOLDEN RULE #8 — Architecture Integrity Lock

Нельзя вносить изменения в архитектуру:

без PR в ветку docs/architecture

без одобрения владельца архитектуры

без AutoDoc регенерации

без CI валидации диаграмм

🖼 10. PlantUML Diagram: Corporate Git Workflow

Добавь в репо файл:

05_ARCHITECTURE/DIAGRAMS/git_workflow.puml


Содержимое:

@startuml
title META-FOUNDRY • Corporate Git Workflow

actor Developer
rectangle "Local Workspace" {
    Developer --> (Create feature/* branch)
    Developer --> (Commit + Pre-commit checks)
}

rectangle "Remote Repo (GitHub)" {
    (feature/*) --> (Pull Request)
    (docs/*) --> (Pull Request)
    (fix/*) --> (Pull Request)
}

rectangle "CI/CD" {
    (Pull Request) --> (Linting)
    (Pull Request) --> (Security Scan)
    (Pull Request) --> (Unit Tests)
    (Pull Request) --> (AutoDoc Sync)
}

rectangle "Governance" {
    (Pull Request) --> (CODEOWNER Review)
    (CODEOWNER Review) --> (Approval)
}

(Approval) --> (Merge to develop)
(Merge to develop) --> (Release Branch)

(Release Branch) --> (Final Review)
(Final Review) --> (Merge to main)

@enduml

🖼 11. plantuml: Golden Security Rules Overview

Файл:

05_ARCHITECTURE/DIAGRAMS/golden_rules_overview.puml

@startuml
title META-FOUNDRY • Golden Security Rules v0.1

node "Security Doctrine" {
    node "Golden Rule #1" as GR1
    node "Golden Rule #2" as GR2
    node "Golden Rule #3" as GR3
    node "Golden Rule #4" as GR4
    node "Golden Rule #5" as GR5
    node "Golden Rule #6" as GR6
    node "Golden Rule #7" as GR7
    node "Golden Rule #8" as GR8
}

GR1 --> GR2
GR2 --> GR3
GR3 --> GR4
GR4 --> GR5
GR5 --> GR6
GR6 --> GR7
GR7 --> GR8


🏛 12. Архитектурная модель Git-процессов (L1–L4)
Документ: MF-ST-01.01 — Appendix A
Версия модели: 1.0.0
📌 Общее назначение модели L1–L4

Архитектурная модель Git-процессов описывает уровневую декомпозицию инженерного цикла:

L1 — высокоуровневые принципы

L2 — системные компоненты

L3 — процессы и механизмы

L4 — конкретные артефакты, файлы, команды

Эта модель позволяет:

унифицировать работу Git

гарантировать безопасность и воспроизводимость

автоматизировать документацию и контроль

повысить зрелость инженерного цикла

обеспечить связь с MindForge MSDLC

🧱 L1 — Conceptual Level (Концептуальный уровень)

На уровне L1 описываются принципы, политика, рамка управления.

L1.1 Принципы Git Governance

Единство репозитория

Прозрачная история

Обязательный review

Нулевые секреты

Security-by-Default

Автоматизация документации

L1.2 Системные роли

Owner (Архитектор / Техдир) — владелец всех критических веток

Maintainer — управляет merge-процессом

Contributor — вносит изменения через PR

AutoDoc Engine — документирование

Security Scanner / CI — непрерывный контроль

L1.3 Основные потоки управления

поток разработки (dev flow)

поток документации (docs flow)

поток релизов (release flow)

поток безопасности (sec flow)

поток автоматизации (autodoc flow)

🏗️ L2 — System Level (Системный уровень)

Здесь определяются компоненты Git-процесса как элементы архитектуры.

L2.1 Компоненты Git-системы
Компонент	Описание
main branch	стабильный релизный контур
develop branch	интеграционный контур
feature/ branches*	производственные линии разработки
docs/ branches*	контур документации
hotfix/ branches*	аварийный контур
release/ branches*	релизный контур
CI pipeline	автоматические проверки
Security pipeline	контроль ИБ
AutoDoc pipeline	обновление документации
CODEOWNERS	распределение ответственности
pre-commit hooks	первый уровень защиты
.gitignore	барьер от утечек
scripts/security_check.py	экспертный уровень ИБ
L2.2 Связи между компонентами

feature → develop → release → main

docs/* → develop → main

hotfix/* → main → develop

AutoDoc → docs → develop

CI & Security контролируют каждый merge

⚙️ L3 — Process Level (Процессный уровень)

Это полная логика поведения системы, её процессы, протоколы и правила выполнения.

L3.1 Процесс разработки (feature-flow)

1. Создание задачи в issue tracker
2. Создание ветки feature/
3. Разработка + pre-commit
4. PR → develop
5. CI/Lint/Security checks
6. Review by CODEOWNERS
7. Squash-and-merge
8. AutoDoc обновляет документацию**

L3.2 Процесс документирования (docs-flow)

1. Создание ветки docs/
2. Обновление архитектуры / стандартов / описаний
3. PR → develop
4. AutoDoc запускает рендеринг
5. Merge → develop
6. Sync → main (релиз документации)**

L3.3 Процесс релиза (release-flow)

Создание release/vX.Y.Z

Code freeze

CI/Tests/SAST

Финальное review архитектуры

Merge release → main

Tag

Release Notes

AutoDoc формирует версию документации

Merge main → develop (разблокировка разработки)

L3.4 Процесс критических исправлений (hotfix-flow)

Создать hotfix/* от main

Быстрое исправление

PR → main

CI проверки (ускоренные)

Merge → main

Cherry-pick в develop

Document update через AutoDoc

L3.5 Процесс безопасности (security-flow)

Выполняется:

при каждом коммите (pre-commit)

каждом PR (CI)

каждый день в 03:00 (security pipeline)

Проверяется:

секреты

уязвимости Python

запрещённые паттерны

структура репозитория

архитектурная целостность

подлинность автора коммита

🔍 L4 — Implementation Level (Уровень реализации)

Это — конкретные файлы, команды, примеры, конфигурации, артефакты.

L4.1 Команды Git
Создание ветки:
git switch -c feature/autodoc-engine

Создание релиза:
git switch -c release/v0.3.0

Hotfix:
git switch -c hotfix/llm-client-bug

Merge:
git merge --squash feature/autodoc-engine

L4.2 Структура файлов безопасности

Файлы:

.gitignore

.pre-commit-config.yaml

SECURITY.md

CODEOWNERS

.github/workflows/security.yaml

scripts/security_check.py

L4.3 Автоматизация

AutoDoc запускается так:

python autodoc/scripts/build_all.py


CI запускается так:

gh workflow run ci.yaml

L4.4 PlantUML-диаграммы

Файлы:

05_ARCHITECTURE/DIAGRAMS/git_workflow.puml
05_ARCHITECTURE/DIAGRAMS/golden_rules_overview.puml
05_ARCHITECTURE/DIAGRAMS/git_l1_l4_model.puml


Третья диаграмма (новая) — готовая ниже:

🖼 PlantUML: Архитектурная модель Git-процессов L1–L4

Создаём:

05_ARCHITECTURE/DIAGRAMS/git_l1_l4_model.puml


Содержимое:

@startuml
title META-FOUNDRY • Git Process Architecture (L1–L4)

package "L1 — Conceptual" {
  [Principles]
  [Roles]
  [Governance Model]
}

package "L2 — System" {
  [Branches]
  [CI/CD Components]
  [Security Components]
  [Documentation System]
}

package "L3 — Process" {
  [Feature Flow]
  [Docs Flow]
  [Release Flow]
  [Hotfix Flow]
  [Security Flow]
}

package "L4 — Implementation" {
  [Git Commands]
  [Files & Configs]
  [Workflows YAML]
  [Scripts]
}

[Principles] --> [Branches]
[Roles] --> [Branches]

[Branches] --> [Feature Flow]
[Branches] --> [Docs Flow]
[Branches] --> [Release Flow]

[CI/CD Components] --> [Feature Flow]
[Security Components] --> [Security Flow]

[Feature Flow] --> [Git Commands]
[Docs Flow] --> [Files & Configs]
[Release Flow] --> [Workflows YAML]
[Security Flow] --> [Scripts]

@enduml


🔚 Футер корпоративного стандарта
<div align="center">
META-FOUNDRY ENGINEERING PLATFORM

Корпоративный стандарт разработки и безопасности.
Документ MF-ST-01.01. Версия 1.0.0
© 2023–2025 META-FOUNDRY. Все права защищены.

</div>